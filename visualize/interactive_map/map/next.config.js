/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/global_snowmelt_runoff_onset',
  assetPrefix: '/global_snowmelt_runoff_onset',
  reactStrictMode: true,
}

module.exports = nextConfig
